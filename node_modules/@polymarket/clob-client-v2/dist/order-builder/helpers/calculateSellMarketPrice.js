import { OrderType } from "../../types/clob.js";

//#region src/order-builder/helpers/calculateSellMarketPrice.ts
/**
* calculateSellMarketPrice calculates the market price to sell a shares
* @param positions
* @param amountToMatch sells to share
* @returns
*/
const calculateSellMarketPrice = (positions, amountToMatch, orderType) => {
	if (!positions.length) throw new Error("no match");
	let sum = 0;
	for (let i = positions.length - 1; i >= 0; i--) {
		const p = positions[i];
		sum += parseFloat(p.size);
		if (sum >= amountToMatch) return parseFloat(p.price);
	}
	if (orderType === OrderType.FOK) throw new Error("no match");
	return parseFloat(positions[0].price);
};

//#endregion
export { calculateSellMarketPrice };
//# sourceMappingURL=calculateSellMarketPrice.js.map