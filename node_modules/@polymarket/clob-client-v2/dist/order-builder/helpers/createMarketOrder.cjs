const require_signer = require('../../signing/signer.cjs');
const require_config = require('../../config.cjs');
const require_buildMarketOrderCreationArgs = require('./buildMarketOrderCreationArgs.cjs');
const require_signatureTypeV2 = require('../../order-utils/model/signatureTypeV2.cjs');
const require_buildOrder = require('./buildOrder.cjs');
const require_roundingConfig = require('./roundingConfig.cjs');

//#region src/order-builder/helpers/createMarketOrder.ts
const createMarketOrder = async (eoaSigner, chainId, signatureType, funderAddress, userMarketOrder, options, version) => {
	const eoaSignerAddress = await require_signer.getSignerAddress(eoaSigner);
	const maker = funderAddress === void 0 ? eoaSignerAddress : funderAddress;
	const signerForOrder = signatureType === require_signatureTypeV2.SignatureTypeV2.POLY_1271 ? maker : eoaSignerAddress;
	const contractConfig = require_config.getContractConfig(chainId);
	const orderData = await require_buildMarketOrderCreationArgs.buildMarketOrderCreationArgs(signerForOrder, maker, signatureType, userMarketOrder, require_roundingConfig.ROUNDING_CONFIG[options.tickSize], version);
	let exchangeContract;
	switch (version) {
		case 1:
			if (signatureType === require_signatureTypeV2.SignatureTypeV2.POLY_1271) throw new Error(`signature type POLY_1271 is not supported for v1 orders`);
			exchangeContract = options.negRisk ? contractConfig.negRiskExchange : contractConfig.exchange;
			break;
		case 2:
			exchangeContract = options.negRisk ? contractConfig.negRiskExchangeV2 : contractConfig.exchangeV2;
			break;
		default: throw new Error(`unsupported order version ${version}`);
	}
	return require_buildOrder.buildOrder(eoaSigner, exchangeContract, chainId, orderData, version);
};

//#endregion
exports.createMarketOrder = createMarketOrder;
//# sourceMappingURL=createMarketOrder.cjs.map